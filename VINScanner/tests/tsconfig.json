{
    "compilerOptions": {
      "lib": ["dom", "es2015"]
    },
    "include": [
      "./*.ts", // or the relevant directory if placed elsewhere
      "./global.d.ts"   // Ensure this path matches where your global.d.ts is
    ]
  }